assumeinit 本质问题在于可能将为初始化的转为初始化的

必须确信初始化的情况才能使用


由此看来 代码 1 和其他处于一个pattern
同样的 除了78都是相同情况的 就改了改类型
虽然6选择定义后加了个传递指针来初始化，但其实没区别。
10 完全没用

代码7有错误，已经修改了

这里可以认为 除了7 8以外 其他的都一样属于一个pattern，
这里我认为可以 78和其他的分开为两个pattern，当然也可以认为

78自己是有价值，其他的都低价值
78 结合 MaybeUninit::uninit().assume_init()

但是这里都放到有价值里了，
因为assumeinit我认为是一个可以进行提示的api，也就是unsafe到safe改法可以使用智能指针的方式包裹
但是这里需要assumeinit的实际场景考证，