from_raw_parts 的意义是， 从ptr处截取长度为len的slice

1是极简版本，相当于长度为1的数组，2是长度为5的数组，1 2 3 4本质没区别
当然1用了一个slice::from_ref的api 用法不同
1 可以一个pat， 
2 3 4 一个pat 但是4改法不同，可以看作同pat下改法可变
5 作一个pat 问题已经截然不同，要接两个slice到一起

6 没表达清楚，在safe代码里面 v 和 v_new 完全没有联系

7 是可以作为一个pat，但是用到了别的unsafe

8跟6基本一致 safe的8.rs不对

整体比较清晰，都放到高价值